import { createAction } from 'redux-actions';
export const addMap = createAction('CREATE_ACTION');
export const textUpdate = createAction('TEXT_UPDATE');
export const changePointList = createAction('CHANGE_POINT_IST');
export const addPolyline = createAction('ADD_POLYLINE');